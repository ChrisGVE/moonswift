## moonswift.toml — structured-wildcard fixture
##
## Drives: IntegrationTests / SourceStore — wildcard JSONPath producing multiple fragments
## $.handlers.* matches all string values under "handlers", yielding two loaded events.

lua_version = "5.4"

[[source]]
path = "wildcard.yaml"

  [[source.field]]
  jsonpath = "$.handlers.*"
