## config.yaml — error-unresolved-path fixture
##
## A valid YAML document; $.nonexistent.path does not exist here, so the
## JSONPath evaluation returns zero matches (unresolved-path error case).
scripts:
  init: "print('hello')"
