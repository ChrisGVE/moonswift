## moonswift.toml — error-unresolved-path fixture
##
## Drives: IntegrationTests / SourceStore — unresolved JSONPath warning
## The designated JSONPath matches no nodes in the document.
## Expected: SourceLoadEvent.failed with state .failed(Diagnostic) and message starting "⚠"

lua_version = "5.4"

[[source]]
path = "config.yaml"

  [[source.field]]
  jsonpath = "$.nonexistent.path"
