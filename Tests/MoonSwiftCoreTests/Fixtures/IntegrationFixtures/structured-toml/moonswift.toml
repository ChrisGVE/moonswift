## moonswift.toml — structured-toml fixture
##
## Drives: IntegrationTests / SourceStore — TOML structured-file loading
## Designates a string field inside an array-of-tables entry.

lua_version = "5.4"

[[source]]
path = "config.toml"

  [[source.field]]
  jsonpath = "$.hooks[0].script"
