## moonswift.toml — parser-hostile-chunkname fixture
##
## Drives: IntegrationTests / LintService — hostile chunk name characters
## Script: Uses characters that look like YAML/TOML long-bracket markers ("]:",
##         "]:") in a string literal, exercising the luacheck long-string encoder.
## Expected: syntaxPrePass returns nil (code is valid), lint produces diagnostics
##           only for actual Lua issues (the hostile chars are inside a string).

lua_version = "5.4"

[[source]]
path = "hostile-chunkname.lua"
