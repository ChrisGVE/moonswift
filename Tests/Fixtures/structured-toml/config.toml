## config.toml — structured-toml fixture
##
## A TOML file with an array-of-tables ([[ hooks ]]).
## $.hooks[0].script is designated in moonswift.toml.

[[hooks]]
name = "startup"
script = "print('toml hook')"

[[hooks]]
name = "shutdown"
script = "return 0"
