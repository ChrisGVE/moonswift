## moonswift.toml — error-non-string-field fixture
##
## Drives: IntegrationTests / SourceStore — non-string field warning
## The designated JSONPath resolves to an integer, not a string.
## Expected: SourceLoadEvent.failed with state .failed(Diagnostic) containing "expected string"

lua_version = "5.4"

[[source]]
path = "config.json"

  [[source.field]]
  jsonpath = "$.version"
